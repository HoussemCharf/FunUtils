vowels = [ 'a', 'e', 'i', 'o', 'u']

word = input('type in a word for vowels')


for letters in word:
    if letters in vowels:
       found.setdefault(letter,0)
       found[letters] += 1 
       
for k, v in sorted(found.items()):
    print(k, 'was found', v, 'times')
